These are whole-file copies of html5lib tree-construction fixtures whose `#errors` contradicted the WHATWG living standard.
The runner (`source_path/1` in `test/support/html5lib_tree_construction_tests.ex`) reads a file here in place of the submodule copy.
Each patched case has a `#spec` citation; the `text:` lines are the text's count.
To correct another fixture, copy it here and fix `#errors`. Do not update `test/html5lib-tests` past `9329e64` (upstream deleted these files).
